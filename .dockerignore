# The .dockerignore file is used to exclude sensitive information from being
# copied into the docker image when using mass copy commands like:
# WORKDIR /usr/src/app
# COPY . .

# git and cache folders
.git
.cache

# markdown and class files
*.md
**/*.class

# logs directory
logs/

# keys and credentials
keys.json

# the Dockerfile itself (optional)
Dockerfile

# custom exceptions
00_test
